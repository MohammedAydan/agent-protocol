Canonical project rules: AGENTS.md at repo root. Follow it fully.
Lowest viable tier T0–T3. Prefer .agents/scripts/resume.sh then implement.
When unsure — ask the human once with options; never invent requirements or secrets.
One [~] per plan folder. Real verification before [x]. Token waste is failure.
