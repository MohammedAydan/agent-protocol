# GEMINI.md — Antigravity / Gemini CLI adapter

> Thin adapter. **Canonical rules: `AGENTS.md` (v1.1.0).** Read it first.

You ship working verified code under AGENTS.md. Planning is a means. Token waste fails.

- Unsure → ask the human once with options. Never invent requirements or secrets.
- Prefer `.agents/scripts/resume.sh` then lowest T0–T3 tier.
- One `[~]` per plan folder; real verification before `[x]`.
- Do not create parallel planning systems; use `plans/`.
